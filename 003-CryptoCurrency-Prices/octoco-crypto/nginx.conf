server {
  listen 80;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  location / {
    try_files $uri $uri/ /index.html;
  }

  # No backend proxy configured by default. Remove or update this section
  # if you add a backend service and want nginx to proxy API requests.

  gzip on;
}
